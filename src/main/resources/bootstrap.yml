server:
  port: 8090
  
spring:
  application:
    name: patron-dto
  config:
    name: ${spring.application.name}
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active:
    - DEV